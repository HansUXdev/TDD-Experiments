//////////////////////////////////////////////
/// Things to check before reaching out
/// 1). Check the GET routes
/// 2). Check api POST routes
/// 3). Check the PUT routes
/// 4). Check the DELETE
/// 5). Check the Authentication
//////////////////////////////////////////////

describe("API", function() {
  it("should be able to GET things on request", function() {
    // expect( fs.existsSync('.htaccess') )
    // .to.equal(true);
  });
  it("should be able to POST things on request", function() {
  });
  it("should be able to PUT things on request", function() {
  });
  it("should be able to DELETE things on request", function() {
  });
});

