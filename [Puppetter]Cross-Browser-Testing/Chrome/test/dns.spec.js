describe("DNS", function() {
  it("IP should be able to match A records", function() {
    // await page.goto(siteUrl);
  });
  it("firewall should point to the proper A record", function() {
    // await page.goto(siteUrl);
  });
});